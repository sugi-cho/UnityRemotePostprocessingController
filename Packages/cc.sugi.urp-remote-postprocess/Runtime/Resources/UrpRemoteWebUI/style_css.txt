:root {
  --bg: #10141a;
  --bg-elev: #1a212b;
  --card: #202938;
  --text: #f2f5f9;
  --muted: #9eb0c7;
  --accent: #33c27f;
  --danger: #e95656;
  --border: #344156;
}

* {
  box-sizing: border-box;
}

html,
body {
  width: 100%;
  max-width: 100%;
  margin: 0;
  overflow-x: hidden;
}

body {
  color: var(--text);
  background:
    radial-gradient(circle at 20% -20%, #2a3850, transparent 50%),
    radial-gradient(circle at 120% 20%, #18313f, transparent 40%),
    var(--bg);
  font-family: "Segoe UI", "Hiragino Sans", sans-serif;
  -webkit-text-size-adjust: 100%;
}

body.gesture-lock {
  overflow: hidden;
}

.header {
  position: sticky;
  top: 0;
  z-index: 10;
  padding: 12px max(12px, env(safe-area-inset-right)) 12px max(12px, env(safe-area-inset-left));
  background: rgba(16, 20, 26, 0.94);
  backdrop-filter: blur(8px);
  border-bottom: 1px solid var(--border);
}

h1 {
  margin: 0;
  font-size: 18px;
}

.status {
  margin-top: 4px;
  color: var(--muted);
  font-size: 12px;
}

.auth-panel {
  margin-top: 10px;
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr) auto auto auto;
  gap: 8px;
}

.auth-panel.is-login {
  grid-template-columns: minmax(0, 1fr) auto;
}

.auth-panel.is-setup {
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr) auto;
}

.auth-panel.is-authenticated {
  grid-template-columns: auto;
  justify-content: start;
}

.auth-panel button {
  white-space: nowrap;
}

.auth-panel.is-login #authPasswordConfirm,
.auth-panel.is-login #authSetupBtn,
.auth-panel.is-login #authLogoutBtn,
.auth-panel.is-setup #authLoginBtn,
.auth-panel.is-setup #authLogoutBtn,
.auth-panel.is-authenticated #authPassword,
.auth-panel.is-authenticated #authPasswordConfirm,
.auth-panel.is-authenticated #authSetupBtn,
.auth-panel.is-authenticated #authLoginBtn {
  display: none;
}

.preset-bar {
  margin-top: 10px;
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr) repeat(4, auto);
  gap: 8px;
}

.view-switch {
  margin-top: 10px;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
}

.view-switch button.is-active {
  background: var(--accent);
  color: #0d1b14;
  border-color: #58d99e;
}

main {
  padding: 12px max(12px, env(safe-area-inset-right)) calc(12px + env(safe-area-inset-bottom)) max(12px, env(safe-area-inset-left));
  display: grid;
  gap: 10px;
  overflow-x: hidden;
}

input, button, select {
  min-height: 44px;
  min-width: 0;
  border-radius: 10px;
  border: 1px solid var(--border);
  background: var(--bg-elev);
  color: var(--text);
  padding: 0 10px;
  touch-action: manipulation;
}

button {
  background: #2c3b52;
}

button.danger {
  background: #5a2e34;
  border-color: #8d3f4a;
}

.component {
  border: 1px solid var(--border);
  border-radius: 12px;
  background: var(--card);
  overflow: hidden;
}

.volume {
  border: 1px solid #3f506d;
  border-radius: 14px;
  background: rgba(23, 30, 42, 0.9);
  overflow: hidden;
}

.volume-header {
  list-style: none;
  width: 100%;
  padding: 12px;
  cursor: pointer;
  background: #223047;
  font-weight: 600;
}

.volume-header::-webkit-details-marker {
  display: none;
}

.volume-body {
  padding: 10px;
  display: grid;
  gap: 10px;
}

.component-header {
  list-style: none;
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px;
  cursor: pointer;
  background: #283246;
}

.component-header::-webkit-details-marker {
  display: none;
}

.component-body {
  padding: 10px;
  display: grid;
  gap: 12px;
}

.field {
  display: grid;
  gap: 6px;
}

.field label {
  font-size: 13px;
  color: var(--muted);
}

.field-head {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.override-wrap {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  font-size: 12px;
  color: var(--muted);
}

.component-override {
  font-size: 11px;
}

.override-wrap input {
  min-height: 18px;
  width: 18px;
}

.field.disabled {
  opacity: 0.55;
}

.field-content {
  display: grid;
  gap: 8px;
}

.row {
  display: flex;
  gap: 8px;
  align-items: center;
}

.row input[type="range"] {
  flex: 1;
  touch-action: none;
}

.small {
  width: 88px;
}

.color-preview {
  width: 24px;
  height: 24px;
  border-radius: 6px;
  border: 1px solid var(--border);
}

.channel-tabs {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 6px;
}

.channel-tabs button {
  min-height: 36px;
  border-radius: 8px;
  background: #25344a;
}

.channel-tabs button.is-active {
  background: #4c6f95;
  border-color: #6b8fb8;
}

.trackball {
  display: grid;
  grid-template-columns: 130px 1fr;
  gap: 10px;
  align-items: center;
}

.trackball-wheel {
  position: relative;
  width: 120px;
  height: 120px;
  border-radius: 999px;
  border: 2px solid #2d374a;
  background:
    radial-gradient(circle at center, rgba(28, 35, 48, 0.92) 42%, transparent 44%),
    conic-gradient(from 0deg, #ff0000, #ff00ff, #0000ff, #00ffff, #00ff00, #ffff00, #ff0000);
  overflow: hidden;
  touch-action: none;
}

.trackball-picker {
  position: absolute;
  inset: 0;
  opacity: 0;
  cursor: pointer;
  touch-action: none;
}

.trackball-marker {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 18px;
  height: 18px;
  border-radius: 999px;
  border: 2px solid #e8edf7;
  transform: translate(-50%, -50%);
  box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.35);
  background: #fff;
}

.trackball-sliders {
  display: grid;
  gap: 4px;
}

.trackball-sliders label {
  font-size: 12px;
  color: var(--muted);
}

.trackball-values {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 6px;
  margin-top: 2px;
}

.trackball-value-item {
  display: grid;
  gap: 2px;
  min-width: 0;
}

.trackball-value-item span {
  font-size: 11px;
  color: var(--muted);
}

.trackball-value-input {
  min-height: 34px;
  padding: 0 8px;
  font-size: 12px;
}

@media (max-width: 760px) {
  .auth-panel {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .auth-panel.is-setup #authSetupBtn,
  .auth-panel.is-login #authLoginBtn {
    grid-column: 1 / 3;
  }

  .preset-bar {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }

  .preset-bar > :nth-child(1) {
    grid-column: 1 / 3;
  }

  .preset-bar > :nth-child(2) {
    grid-column: 3 / 5;
  }

  .preset-bar button {
    width: 100%;
  }

}
